model User{
    id          Int             @id @default(autoincrement()) @map("_id")
    username    String          @unique
    email       String          @unique
    password    String
    avatar      String?

    sentMessage  Message[]
    workspaces   WorkspaceMember[]
}