model WorkSpace{
    id          Int     @id @default(autoincrement()) @map("_id")
    name        String
    description String? @default("")

    members     User[]

    joinCode    String @unique

    channel     Channel[]
}