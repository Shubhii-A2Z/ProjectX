model WorkSpace{
    id          Int     @id @default(autoincrement()) @map("_id")
    name        String  @unique
    description String? @default("")

    membersId   Int[]
    members     User[]

    role        Role

    joinCode    String

    channelId   Int[]
    channel     Channel[]
}