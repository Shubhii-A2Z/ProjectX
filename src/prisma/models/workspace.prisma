model WorkSpace{
    id          Int     @id @default(autoincrement()) @map("_id")
    name        String  @unique
    description String? @default("")

    members     User[]

    joinCode    String

    channel     Channel[]
}